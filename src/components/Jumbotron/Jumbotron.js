import React from "react";
import "./Jumbotron.css";

const Jumbotron = () => (
  <div className="jumbotron">
    <h1>Classic Mustangs Memory Click Game</h1>
    <p>Click on an image to increase the score. Do not click the image more than once, otherwise the game will reset. Try to beat the top score!</p>
  </div>
);

export default Jumbotron;